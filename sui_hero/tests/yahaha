<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sui Hero Arena | dApp Dashboard</title>

  <!-- Tailwind via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

  <!-- dApp Kit styles -->
  <link rel="stylesheet" href="https://unpkg.com/@mysten/dapp-kit@latest/dist/index.css" />

  <style>
    :root {
      --sui-blue: #6fbcf0;
      --sui-deep: #1a202c;

      /* palette cerah */
      --bg-main: #f3f4f6;
      --card-bg: #ffffff;
      --accent: #6366f1;
      --text-main: #111827;
    }

    body {
      font-family: 'Plus Jakarta Sans', sans-serif;
      background-color: var(--bg-main);
      color: var(--text-main);
      background-image:
        radial-gradient(at 0% 0%, hsla(210, 100%, 90%, 1) 0, transparent 55%),
        radial-gradient(at 100% 100%, hsla(230, 100%, 88%, 1) 0, transparent 55%);
      background-attachment: fixed;
      margin: 0;
    }

    .font-mono {
      font-family: 'JetBrains Mono', monospace;
    }

    /* Animations */
    @keyframes level-up-pulse {
      0% {
        border-color: #4f46e5;
        box-shadow: 0 0 0 0 rgba(79, 70, 229, 0.35);
      }
      70% {
        border-color: #818cf8;
        box-shadow: 0 0 20px 10px rgba(79, 70, 229, 0);
      }
      100% {
        border-color: #4f46e5;
        box-shadow: 0 0 0 0 rgba(79, 70, 229, 0);
      }
    }
    .animate-level-up {
      animation: level-up-pulse 2s infinite;
    }

    @keyframes fade-in-up {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .fade-in {
      animation: fade-in-up 0.4s ease-out forwards;
    }

    @keyframes slide-in {
      from { opacity: 0; transform: translateX(100%); }
      to { opacity: 1; transform: translateX(0); }
    }
    .animate-slide-in {
      animation: slide-in 0.3s ease-out forwards;
    }

    /* Glass card lebih terang */
    .glass {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(148, 163, 184, 0.6);
    }

    .text-gradient {
      background: linear-gradient(to right, #6366f1, #6fbcf0);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    /* Scrollbar cerah */
    ::-webkit-scrollbar {
      width: 6px;
    }
    ::-webkit-scrollbar-track {
      background: #e5e7eb;
    }
    ::-webkit-scrollbar-thumb {
      background: #cbd5f5;
      border-radius: 3px;
    }
    ::-webkit-scrollbar-thumb:hover {
      background: #a5b4fc;
    }

    /* Tabs: UNITS / SUMMON / HISTORY */
    .nav-tabs {
      border-radius: 9999px;
      background-color: #9ca3af;
      padding: 2px;
    }
    .nav-tabs button {
      color: #111827;
      font-weight: 600;
      font-size: 0.75rem;
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }
    .nav-tabs button.tab-active {
      background-color: #111827;
      color: #82aad1 !important;
    }
    .nav-tabs button:hover:not(.tab-active) {
      color: #020617;
    }

    /* Header / top bar biru muda */
    header,
    .top-bar,
    .navbar {
      background-color: #dbeafe;          /* biru muda */
      border-bottom: 1px solid #93c5fd;   /* biru sedikit lebih tua */
    }

    header > div,
    .top-bar > div,
    .navbar > div {
      background-color: transparent !important;
    }

    /* ==== Teks kanan & kiri unit supaya jelas ==== */

    /* Sesuaikan dengan layout-mu;
       ini menarget area list unit & detail unit. */
    .units-layout,
    .units-sidebar,
    .units-details {
      color: #0f172a; /* teks biru tua, kontras di bg terang */
    }

    .units-layout p,
    .units-layout span,
    .units-sidebar p,
    .units-sidebar span,
    .units-details p,
    .units-details span {
      color: #0f172a;
    }

    /* Override teks abu-abu Tailwind yang biasanya pudar di light theme */
    .text-slate-400,
    .text-slate-500,
    .text-gray-400,
    .text-gray-500 {
      color: #0f172a !important;
    }

    /* Judul unit / teks penting dengan sedikit shadow */
    .unit-title,
    .unit-subtitle {
      color: #0f172a;
      text-shadow: 0 1px 2px rgba(148, 163, 184, 0.7);
    }
  </style>

  <!-- React & friends via import map -->
  <script type="importmap">
  {
    "imports": {
      "react": "https://esm.sh/react@^19.2.3",
      "react-dom/": "https://esm.sh/react-dom@^19.2.3/",
      "react/": "https://esm.sh/react@^19.2.3/",
      "@google/genai": "https://esm.sh/@google/genai@^1.34.0"
    }
  }
  </script>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/index.tsx"></script>
</body>
</html>
